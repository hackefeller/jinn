# Templates

This directory contains command, skill, and agent templates that are tool-agnostic.

## Structure

- `commands/` - Command templates (slash commands)
- `skills/` - Skill templates (domain expertise)
- `agents/` - Agent templates (AI personas)

## Usage

Templates are registered in the template registry and used by the generator to create tool-specific files.
